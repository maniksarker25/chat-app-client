import { Box } from "@mui/material";

const ChatPage = () => {
  return (
    <Box
      sx={{
        backgroundColor: "gray",
        width: "100%",
        height: "100%",
      }}
    >
      This is chat page
    </Box>
  );
};

export default ChatPage;
